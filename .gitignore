# Dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js / build
.next/
out/
build/
dist/

# Testing
coverage/
*.lcov

# Environment / secrets — never commit real secrets
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
.env.staging.local
# Keep the template:
# .env.example is intentionally tracked

# Vercel
.vercel

# Supabase local
supabase/.branches
supabase/.temp

# Debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# TypeScript
*.tsbuildinfo

# OS / editor
.DS_Store
*.pem
.idea/
.vscode/*
!.vscode/extensions.json

# Misc
*.swp
*.swo
.turbo

# Claude Code — local-only session config, never shared/versioned.
# CLAUDE.md and any other intentionally-versioned project-level Claude
# documentation/config stay tracked; only these local files are ignored.
.claude/settings.local.json
.claude/launch.json
